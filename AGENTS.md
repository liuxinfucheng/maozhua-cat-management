# 猫爪猫咪管理后台协作约定

本说明仅适用于本项目目录及其子目录。

## GitHub 源码同步

- 远程仓库：https://github.com/liuxinfucheng/maozhua-cat-management
- 目标分支：`main`。
- 用户要求同步或上传代码时，默认使用已授权的 GitHub 插件接口，不使用本机 `git push`。GitHub 插件授权不代表本机 Git 已配置登录凭据。
- 本地与远程曾分别创建提交，提交历史可能不一致；不要为同步而自动合并历史、重置本地分支或强制推送。
- 同步前检查本地工作区、远程最新提交和文件差异，包含用户要求同步的最新本地修改，不要遗漏未提交或新增的源码文件。
- 基于远程最新提交创建新提交，以非强制方式更新 `main`，保留远程历史。遇到远程独立修改时先核对，不能直接覆盖。
- 仅同步本项目源码和必要配置，不上传 `本地数据/`、数据库、照片、`node_modules/`、构建产物、凭据或外层目录的其他项目。
- 同步后核对远程提交及文件内容，并提供提交链接。不要声称本地已建立远程跟踪关联，除非实际完成了该配置。
- 如果 GitHub 插件不可用或未授权，说明缺少的连接，不擅自改用强制推送等方式。

## Windows 构建

- 工作流位于 `.github/workflows/build-windows.yml`，保持仅 `workflow_dispatch` 手动触发。
- 默认只同步代码，不触发云端打包，不添加 `push`、定时或其他自动构建触发器。
- 用户会自行在 GitHub Actions 手动构建；只有用户明确要求本次构建时，才执行构建操作。

## 操作范围

- 本说明记录操作方式，不代表每次修改代码后都要自动上传。仅在用户要求同步时进行远程写入。
- 用户当前明确的新指令优先于本说明。
